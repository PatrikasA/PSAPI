@{
    ViewData["Title"] = "Pagrindinis puslapis";
}

<div class="phone">
    <div class="text-center">
        <h1 class="display-4">Sveiki atvykę į restorano valdymo sistemą!</h1>
        <p>Čia skaniausi burgeriai ir šviežiausios salotos.</p>
    </div>

    <br />
    <p class="center">Prisijunkite arba užsiregistruokite:</p>
    <div class="row justify-content-center">
        <div class="col-6 col-md-4">
            <a class="mygtukas btn btn-outline-dark btn-block d-flex align-items-center justify-content-center"
               href="@Url.Action("Login", "User")">Prisijungti</a>
        </div>
        <div class="col-6 col-md-4">
            <a class="mygtukas btn btn-outline-dark btn-block d-flex align-items-center justify-content-center"
               href="@Url.Action("Register", "User")">Registruotis</a>
        </div>
        <div class="col-6 col-md-4">
            <a class="mygtukas btn btn-outline-dark btn-block d-flex align-items-center justify-content-center"
               href="@Url.Action("GoogleLogin", "User")">Prisijungti su Google</a>
        </div>
    </div>
</div>
