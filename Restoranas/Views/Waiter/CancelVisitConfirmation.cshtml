@{
    ViewData["Title"] = "Cancel Visit Confirmation";
}

<h2>Are you sure you want to cancel this visit?</h2>

<form method="post" action="@Url.Action("CancelVisit", "Waiter", new { id = ViewBag.VisitId })">
    <button type="submit">Yes, Cancel It</button>
</form>
